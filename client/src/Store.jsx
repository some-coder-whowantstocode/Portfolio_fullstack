import { proxy } from "valtio";

export const HOME = proxy({
    start:0,
    end:0
})

export const ABOUT = proxy({
    start:0,
    end:0
})

export const PROJECT = proxy({
    start:0,
    end:0
})

export const CONTACT = proxy({
    start:0,
    end:0
})

export const Responsive =proxy({
    width:600
})
